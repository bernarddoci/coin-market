import React from 'react';

const Footer = props => (
    <div className="footer text-center">
        <p>Tv Series-App <small>Copyright &copy; {props.date}</small></p>
    </div>
);

export default Footer;